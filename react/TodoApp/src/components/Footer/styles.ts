import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    width: '100%',
    height: 100,
  },
  plusIconContainer: {
    marginLeft: 'auto',
    marginRight: 10,
  },
});
